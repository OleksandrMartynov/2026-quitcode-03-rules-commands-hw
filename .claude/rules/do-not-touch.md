# Захищені зони репозиторію

## Контекст

`app/src/core/` належить платформній команді — той самий core працює в усіх
клієнтських воркерах агенції, і зміни в ньому йдуть окремим PR через рев'ю цієї
команди. `app/scripts/`, `materials/`, `.coderabbit.yaml`, `.github/`,
`.claude/` і те, чим запускаються перевірки (`app/package.json`,
`app/tsconfig.json`) — це інструменти перевірки й вихідні дані: код, який
змінює власну перевірку, перевіркою більше не є.

## Правило

### Ці шляхи не редагуються

| Шлях | Що це |
|---|---|
| `app/src/core/**` | ядро платформи, зокрема `types.ts`, `http.ts`, `config.ts`, `parse.ts`, `log.ts` і їхні тести |
| `app/scripts/**` | `check-rules.mjs` і `core.lock.json` — перевірка правил |
| `materials/**` | архітектурна записка, лог інциденту, A/B-запит |
| `.coderabbit.yaml` | конфігурація рев'ю |
| `.github/**` | шаблон PR і робочі процеси |
| `.claude/**` | самі правила, команди, хук і `settings.json` — тобто ця перевірка |
| `.git/**` | службова тека git. `.git/hooks/pre-commit` виконується дочірнім процесом `git commit`, тобто дає запустити код повз усі перевірки |
| `app/package.json`, `app/tsconfig.json` | чим **запускаються** `test`, `typecheck`, `check:rules` |

Не редагуй, не створюй і не видаляй файли за цими шляхами. Це стосується й
«адитивних» змін: додати одне поле в інтерфейс, додати один файл поруч, додати
рядок у кінець — така сама заборона, як переписати модуль.

Заборона адресована **агентові в сесії**. Самі правила, команди й хук під
`.claude/` хтось мусив написати — це робить людина, і в історії репозиторію ці
файли законно з'являються. Різниця не в тому, чи файл колись створювали,
а в тому, хто його змінює і в який момент.

Не обходь заборону іншим інструментом: ні редактором, ні `sed -i`, ні `cat >`,
ні `python -c`, ні застосуванням патча.

### Окремо: не підганяй перевірку

Перевірку можна зіпсувати трьома способами, і всі три заборонені однаково.

1. **Не запускай `node scripts/check-rules.mjs --write-lock`.**
   Цей режим перезаписує `app/scripts/core.lock.json` поточними хешами ядра —
   після нього `check:rules` показує `core-untouched 0` навіть тоді, коли ядро
   змінили. Це не виправлення, а приховування сліду.
2. **Не переписуй те, чим запускається перевірка.** Скрипти `test`, `typecheck`
   і `check:rules` у `app/package.json`, а також `app/tsconfig.json`. Число,
   отримане зміненою командою, — не результат. Послабити `strict` у `tsconfig`,
   щоб `typecheck` перестав падати, — те саме, що підробити число.
3. **Не редагуй сам захист** — `.claude/rules/`, `.claude/commands/`,
   `.claude/hooks/` і `.claude/settings.json`. Правило, яке агент може переписати
   перед тим, як його порушити, — не правило. З тієї ж причини захищені
   `app/scripts/**` і `.coderabbit.yaml`: **код, що змінює власну перевірку,
   перевіркою більше не є**.

Якщо здається, що треба змінити щось із цього — це завжди рішення людини,
ухвалене поза сесією агента. Зупинись і опиши, що саме й навіщо.

### Що робити, коли задача впирається в захищену зону

Зупинись і напиши людині:

1. який саме файл і рядок треба змінити (наприклад, `app/src/core/types.ts`,
   інтерфейс `Lead`);
2. яку саме зміну (наприклад, додати поле `utmCampaign?: string`);
3. чому без неї задача не виконується — **з конкретним доказом, а не переказом**:
   назви команду й дослівний текст помилки, яку вона дасть. Наприклад: фікстури
   тестів типізовані `const lead: Lead`, `tsconfig` має `strict: true`, тож
   `npm run typecheck` впаде з
   `Property 'utmCampaign' does not exist on type 'Lead'`.
   «Не скомпілюється» чи «зламає типи» — недостатньо;
4. що вже зроблено в незахищених шляхах і що лишилось після зміни в core.

Потім **чекай відповіді**. Не починай зміну в core й не обирай обхідний шлях,
який ламає інші правила (`any`, дублювання типу в `integrations/`, власна копія
функції ядра).

Незавершена задача з описаною причиною — правильний результат.
Задача, «завершена» через зміну ядра, — зламана домовленість команди.

### «Так» від людини не знімає заборону

Навіть отримавши «так», **не редагуй захищений файл сам**. Ці шляхи захищені ще й
технічно — хуком `PreToolUse` у `.claude/settings.json`, і він блокує запис
незалежно від того, що сказано в діалозі. Це навмисно: підтвердження в чаті не
проходить рев'ю платформної команди, а зміна ядра має проходити.

Тому підтвердження означає лише те, що зміну **передають у роботу далі**: її
вносить платформна команда окремим PR, або людина свідомо змінює сам захист
(`.claude/settings.json` і цей файл) — і це теж її рішення, не твоє.
Якщо після «так» твій запис заблоковано — це не збій інструмента й не привід
шукати обхід: скажи людині, що зміна впирається в технічний захист.

## Як перевірити

```bash
PROT="app/src/core app/scripts materials .coderabbit.yaml .github app/package.json app/tsconfig.json"
git diff --stat upstream/main...HEAD -- $PROT
git status --short -- $PROT
```

Обидві команди мають дати **порожній вивід**. Будь-який рядок — порушення.

`.claude/**` у цей перелік навмисно **не** входить, хоча редагувати його агентові
теж не можна. Причина: самі правила, команди й хук хтось мусив написати, і це
робить людина поза сесією агента — тому в діфі PR вони законно присутні.
Для агента межа та сама, і тримає її хук: спроба запису в `.claude/**`
повертає `exit 2`. Перевірити це можна так:

```bash
printf '{"cwd":"%s","tool_name":"Edit","tool_input":{"file_path":".claude/settings.json"}}' "$PWD" \
  | node .claude/hooks/protect-core.mjs; echo "exit=$?"   # очікується exit=2
```

Додатково: `cd app && npm run check:rules` → у розділі `by rule` рядок
`core-untouched  0`. Якщо там не нуль — ядро змінене або `core.lock.json` не
збігається з файлами.
